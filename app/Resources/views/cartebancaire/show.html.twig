{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cartebancaire</h1>

    <table>
        <tbody>
            <tr>
                <th>Numero</th>
                <td>{{ cartebancaire.numero }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ cartebancaire.code }}</td>
            </tr>
            <tr>
                <th>Solde</th>
                <td>{{ cartebancaire.solde }}</td>
            </tr>
            <tr>
                <th>Iduser</th>
                <td>{{ cartebancaire.idUser }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cartebancaire_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cartebancaire_edit', { 'id': cartebancaire.numero }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
